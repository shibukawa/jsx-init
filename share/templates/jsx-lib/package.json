{
  "name": "{{& name }}",
  "version": "{{& version }}",
  "description": "{{& description }}",
  "scripts": {
    "build": "grunt build",
    "test": "grunt test",
    "doc": "grunt doc"
  },
  "engines" : { "node" : ">=0.8.0" },
  "keywords": [
    "jsx",
    "lib"
  ],
  "main": "./lib/{{& filebasename}}.jsx",
  "homepage": "{{& homepage }}",
  "bugs": {
    "url": "{{& issuetracker }}"
  },
  "directories": {
    "doc": "./doc",
    "lib": "./lib",
    "test": "./test"
  },
  "repository": {
    "type": "{{& repositorytype}}",
    "url": "{{& repository }}"
  },
  "devDependencies": {
    "grunt-contrib-watch": "~0.5.3",
    "grunt": "~0.4.1",
    "grunt-jsx": ">=0.1.4"
  },
  "author": "{{& author }}",
  "license": "{{& licensename }}"
}
